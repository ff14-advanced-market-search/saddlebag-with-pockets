---
name: "Unit Tests"
on: push

jobs:
  "Vitest":
    runs-on: ubuntu-latest
    container: node:22.23.2

    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: yarn install --frozen-lockfile
      - name: Run Tests
        run: yarn test --run
